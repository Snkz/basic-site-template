<div id="section80">
  <h4 id="section" class="rainbow">${link_header}</h4>
  <ul>
    <li id="location">
      <a href="${link}">$text</a> <!-- need to detect if text or html-->
      <del>${text}</del> <!-- detect strike through option -->
    </li>
  </ul>
</div>
